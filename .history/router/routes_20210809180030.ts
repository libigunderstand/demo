
interface routesProp {
    path: string;
    component?: string;
    redirect?: string;
    exact?: boolean;
}

const routes:[routesProp] = [
    {
        path: '/',
        component: ''
    }
]
export default routes